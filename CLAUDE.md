@AGENTS.md
## Git Rules — STRICT
- DO NOT run: git add, git commit, git push, git merge under ANY circumstance
- DO NOT stage files
- Make file changes only — I handle all git operations manually
- After completing each phase, output a summary of changed files and stop